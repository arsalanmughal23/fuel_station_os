You are an expert software architect and code reviewer. I need you to analyze my entire project and sync it with my ERD (Entity Relationship Diagram) and implementation plan.

## Context
I have recently updated:
1. `fuelstationos_erd_v4.mermaid` - My database schema/ERD
2. `implementation_plan.md` - My project implementation roadmap

Now I need to align my entire project codebase with these two documents. I want you to:

## Your Task
1. **READ and UNDERSTAND** both files carefully:
   - `fuelstationos_erd_v4.mermaid` - Understand all entities, relationships, attributes, and data types
   - `implementation_plan.md` - Understand the architecture, modules, features, and implementation phases

2. **ANALYZE my entire project structure** (you have access to all files in the project directory)

3. **PERFORM THE FOLLOWING ACTIONS** (without creating new files - only modify/delete existing ones):

### A. File Cleanup
- Identify and delete files that are:
  - No longer needed based on the new ERD and implementation plan
  - Obsolete or deprecated
  - Redundant or duplicate
  - Not referenced anywhere in the codebase

### B. Code Updates
- Update/modify existing files to align with:
  - New database schema (tables, columns, relationships from ERD)
  - New implementation plan (architecture, modules, features)
  - Correct naming conventions matching the ERD (entity names, field names)
  - Proper relationships (foreign keys, joins) as per ERD
  - Feature implementation order from implementation plan

### C. Code Removal
- Remove code snippets, functions, classes, or modules that:
  - Reference removed/renamed tables or columns
  - Implement features no longer in the plan
  - Use old data structures that don't match ERD
  - Contain deprecated logic

### D. Consistency Checks
- Ensure:
  - All database models match ERD exactly
  - All API endpoints align with implementation plan
  - All service/controller methods reflect the new architecture
  - Configuration files use correct database/schema names
  - Migrations reflect the new schema
  - Environment variables match new requirements

### E. Documentation Updates
- Update inline comments and docstrings to reflect new schema and plan
- Remove outdated TODO comments
- Add necessary TODO comments for missing implementations

## Important Rules
1. **DO NOT create any new files** - Only modify or delete existing ones
2. **Maintain code functionality** - Don't break working features, only align with new requirements
3. **Preserve business logic** - Keep core business logic intact unless it conflicts with new schema
4. **Be thorough** - Check all files: models, controllers, services, routes, middleware, config, migrations, tests, etc.
5. **Provide clear report** - After making changes, provide a summary of:
   - Files deleted (with reasons)
   - Files modified (with key changes)
   - Consistency issues found and fixed
   - Any remaining inconsistencies that need manual attention

## Output Format
Provide your response in this structure:

1. **Analysis Summary**: Overview of findings
2. **Deleted Files**: List with reasons
3. **Modified Files**: List with specific changes
4. **Code Removals**: What code was removed and why
5. **Consistency Fixes**: What was corrected
6. **Remaining Issues**: What needs manual attention
7. **Recommendations**: Additional suggestions

## Begin Analysis
Start by carefully examining the ERD and implementation plan, then systematically review all files in the project. Be thorough and methodical. Show your work and reasoning for each change.

**IMPORTANT**: Remember, you're working in a real project. Be conservative with deletions - if unsure about a file's necessity, mark it for review rather than deleting it.