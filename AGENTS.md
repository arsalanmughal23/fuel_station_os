# Project: My Application

## Project Context
This is an ongoing project that has already been analyzed. The following documents contain the current state and next steps:

## Key Documents
- **`implementation_plan.md`** - The main project roadmap and overall implementation strategy
- **`code_review.md`** - Generated by Sonnet. Contains detailed code review findings, including:
  - Specific issues found in the current codebase
  - Areas that need improvement
  - Recommendations for fixes
- **`sub_plan.md`** - Generated by Gemini based on the code review. Contains:
  - Concrete action items to address the issues from `code_review.md`
  - Step-by-step fixes and improvements
  - Specific tasks that need to be completed
  - (Some initial work may have already been started)

## Previous Work Context
- Other AI models (Sonnet, Gemini) have already done some work on this project
- Review the existing codebase and file modifications to understand what has already been fixed
- Check recent file changes and modifications to avoid duplicating work
- If you find files that have been recently modified, read them to understand the current state
- Cross-reference completed tasks in `sub_plan.md` with actual file changes in the codebase
- If you are unsure whether a specific issue has been addressed, check the relevant files directly

## Agent Instructions

1. **Start by reading all three documents** (`implementation_plan.md`, `code_review.md`, and `sub_plan.md`) to understand the full context.

2. **Check previous work done by other models**:
   - Examine the current codebase to see what changes have already been made
   - Look at file timestamps and recent modifications
   - Compare the issues in `code_review.md` against the current state of the code
   - Identify which issues from `code_review.md` have already been resolved
   - Check if any incomplete fixes need to be completed

3. **Understand the workflow**:
   - `code_review.md` tells you WHAT is wrong or needs improvement
   - `sub_plan.md` tells you HOW to fix it (the action plan)
   - `implementation_plan.md` is the broader project roadmap
   - Previous work may have already started implementing parts of `sub_plan.md`

4. **Begin working through the remaining tasks**:
   - Identify what work has already been completed
   - Start with the next incomplete task from `sub_plan.md`
   - Use `code_review.md` to verify the issues are being properly addressed
   - Check `implementation_plan.md` to ensure fixes align with the overall project goals

5. **Update progress**:
   - After completing each task from `sub_plan.md`, mark it as done
   - If a file is modified, read it again before proposing the next change
   - Keep track of which issues from `code_review.md` have been resolved
   - Document any additional fixes or improvements you make beyond what was planned

6. **Ask for clarification** if anything in the documents is unclear or if you need more context about a specific issue.

7. **Avoid rework**:
   - Before starting a task, check if it has already been completed by previous models
   - If you find work that is partially done, complete it rather than redoing it
   - When in doubt, ask: "Has this issue already been addressed?"